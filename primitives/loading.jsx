import { Html } from '@react-three/drei'

export const Loading = () => {

    return (<>

        <Html>
            <div style={{ zIndex: 1000, position: "fixed", color: "white" }}>Loading...</div>
        </Html>

    </>)
}